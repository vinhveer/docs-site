---
import Layout from "../../layouts/Layout.astro";
import ReadLayout from "../../layouts/ReadLayout.astro";

import { links } from "./define-topic.js";
---
<Layout>
    <ReadLayout title="Tài liệu học Astro" links={links}>
        <h1 class="text-3xl font-bold">Nội dung chính</h1>
        
        <ul class="list-disc pl-6">
            <li>Document structure: Các thẻ nền tảng như <code>&lt;html&gt;</code>, <code>&lt;head&gt;</code>, <code>&lt;body&gt;</code>.</li>
            <li>Metadata: Cung cấp thông tin về trang (ví dụ: <code>&lt;meta&gt;</code>, <code>&lt;title&gt;</code>).</li>
            <li>Semantic HTML: Các thẻ định nghĩa ý nghĩa (<code>&lt;article&gt;</code>, <code>&lt;section&gt;</code>, <code>&lt;nav&gt;</code>).</li>
            <li>Headings and sections: Phân cấp và tổ chức nội dung (<code>&lt;h1&gt;-&lt;h6&gt;</code>, <code>&lt;section&gt;</code>).</li>
        </ul>
        
    </ReadLayout>
</Layout>
